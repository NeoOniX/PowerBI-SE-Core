module.exports = {
    success: /(?:{success})/g,
    error: /(?:{error})/g,
    processID: /(?:{process-id})/g,
    workspaceName: /(?:{workspace-name})/g,
    contentName: /(?:{content-name})/g,
};
