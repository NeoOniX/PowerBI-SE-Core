/**
 * @type {Language}
 */
const en = {
    report: "Report",
    dashboard: "Dashboard",
    loginSuccess: "{success} Valid login !",
    loginFailed: "{error} Invalid login.",
    crawlSuccess: "{processID} > {success} {workspaceName} crawled !",
    crawlError: "{processID} > {error} Error while crawling {workspaceName}",
    scrapSuccess: "{processID} > {success} {contentName} scraped !",
    scrapError: "{processID} > {error} Error while scraping {contentName}",
    uploaderSuccess: "{processID} > {success} Data for {workspaceName} uploaded !",
    uploaderError: "{processID} > {error} Erreur while uploading data for {workspaceName}.",
    processDone: "{processID} > Done !",
    processError: "{processID} > Error.",
};

module.exports = en;
