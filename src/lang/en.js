/**
 * @type {Language}
 */
const en = {
    report: "Report",
    dashboard: "Dashboard",
    loginSuccess : "{success} Valid login !",
    loginFailed : "{error} Invalid login.",
    crawlSuccess : "{process-id} > {success} {workspace-name} crawled !",
    crawlError : "{process-id} > {error} Error while crawling {workspace-name}",
    scrapSuccess : "{process-id} > {success} {content-name} scraped !",
    scrapError : "{process-id} > {error} Error while scraping {content-name}",
    uploaderSuccess : "{process-id} > {success} Data for {workspace-name} uploaded !",
    uploaderError : "{process-id} > {error} Erreur while uploading data for {workspace-name}.",
    processDone: "{process-id} > Done !",
    processError: "{process-id} > Error.",
};

module.exports = en;
